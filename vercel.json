{
  "framework": "nextjs",
  "installCommand": "npm install --no-audit --no-fund",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/health/sync",
      "schedule": "0 8 * * *"
    }
  ]
}
