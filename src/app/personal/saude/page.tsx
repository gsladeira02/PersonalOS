'use client';

import { useEffect, useState } from 'react';
import { TrainerShell } from '@/components/TrainerShell';
import { useTrainerSession } from '@/hooks/useTrainerSession';

export default function SaudePage(){ return <TrainerShell><SaudeContent /></TrainerShell>; }
function SaudeContent(){ const {trainer,supabase}=useTrainerSession(); const [connections,setConnections]=useState<any[]>([]); const [metrics,setMetrics]=useState<any[]>([]); useEffect(()=>{ if(!trainer)return; Promise.all([supabase.from('health_connections').select('*,students(full_name)').eq('trainer_id',trainer.id),supabase.from('health_daily_metrics').select('*,students(full_name)').eq('trainer_id',trainer.id).order('metric_date',{ascending:false}).limit(30)]).then(([c,m])=>{setConnections(c.data||[]);setMetrics(m.data||[]);});},[trainer]); return <><div className="page-head"><div><span className="eyebrow">Relógios e saúde</span><h1>Dados do Relógio</h1><p>Resumo diário, semanal, sono, passos, frequência cardíaca, calorias e alertas simples.</p></div></div><div className="notice">O aluno deve consentir explicitamente antes de qualquer conexão. Dados não geram diagnóstico médico.</div><div className="grid-3" style={{marginTop:18}}><div className="card panel-card"><h3>Apple Health</h3><p>Exige HealthKit em app iOS nativo.</p></div><div className="card panel-card"><h3>Health Connect</h3><p>Exige camada Android nativa.</p></div><div className="card panel-card"><h3>Garmin / Strava</h3><p>OAuth/API com tokens, callbacks, logs e sincronização.</p></div></div><div className="card table-card" style={{marginTop:18}}><table><thead><tr><th>Aluno</th><th>Provider</th><th>Status</th><th>Última sync</th></tr></thead><tbody>{connections.map(c=><tr key={c.id}><td>{c.students?.full_name}</td><td>{c.provider}</td><td>{c.status}</td><td>{c.last_sync_at||'—'}</td></tr>)}</tbody></table></div><div className="card table-card" style={{marginTop:18}}><table><thead><tr><th>Aluno</th><th>Data</th><th>Passos</th><th>Sono</th><th>FC repouso</th></tr></thead><tbody>{metrics.map(m=><tr key={m.id}><td>{m.students?.full_name}</td><td>{m.metric_date}</td><td>{m.steps ?? 'Não informado pelo dispositivo.'}</td><td>{m.sleep_minutes ?? 'Não informado pelo dispositivo.'}</td><td>{m.resting_heart_rate ?? 'Não informado pelo dispositivo.'}</td></tr>)}</tbody></table></div></>; }
